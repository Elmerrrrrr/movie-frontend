import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';


ReactDOM.render(
  <React.StrictMode>

    {/* here we want the landingpage which is linked to the App(the main page) */}

    <App />
  </React.StrictMode>,
  document.getElementById('root')
);



